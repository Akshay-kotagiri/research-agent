# MCP + LangChain tool bridge
langchain-mcp-adapters>=0.1.0

# Core graph framework
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# MCP (Model Context Protocol)
mcp>=1.0.0

# HTTP client for MCP servers
httpx>=0.27.0

# HTML parsing for fetch server
beautifulsoup4>=4.12.0

# Environment variable management
python-dotenv>=1.0.0

# Chatbot UI
streamlit>=1.35.0

# File-system watcher (auto-push on save)
watchdog>=4.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
